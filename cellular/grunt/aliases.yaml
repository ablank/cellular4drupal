default:
    - 'concat'
    - 'newer:imagemin'
build:
    - 'clean:build'
    - 'copy:build'

buildtest:
    - 'clean:buildtest'
    - 'compass'
    - 'uglify:buildtest'
    - 'imagemin:buildtest'
    - 'autoprefixer'
    - 'csscomb'
    - 'cssmin'
