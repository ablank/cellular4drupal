{#
/**
 * @file
 * Default theme implementation for branding
 */
 
{% block content %}
  {% if site_logo %}
    <a href="{{ path('<front>') }}" rel="home">
      <img src="{{ site_logo }}" alt="{{ 'Home'|t }}" />
    </a>
  {% endif %}
  {% if site_name %}
    <a href="{{ path('<front>') }}" title="{{ 'Home'|t }}" rel="home">{{ site_name }}</a>
  {% endif %}
  {{ site_slogan }}
{% endblock %}
#}
{% set site_name = site_name ~ " Home"|t %}
{% block content %}
<div class="logo">
    <a href="{{ path('<front>') }}" title="{{ site_name }}" rel="home">{{ site_name }}</a>
  </div>
{% endblock %}
